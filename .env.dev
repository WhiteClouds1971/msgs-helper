APP_NAME=面杀辅助工具
APP_BASE_API=/api
APP_ENV=development
# 留空 = 请求打到同源的 APP_BASE_API（/api），由 Vite 代理转给后端，浏览器侧无跨域
APP_SERVER_URL=
# 后端地址（server 模块，见 server/src/main/resources/application.yml 的 server.port）
APP_PROXY_TARGET=http://127.0.0.1:8081
